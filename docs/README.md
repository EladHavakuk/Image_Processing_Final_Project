# Docs

The full project report — methodology, dataset, tasks, distortions, restoration,
fine-tuning, results, and a detailed troubleshooting/process log — lives in the
[root README.md](../README.md).

This folder is kept separate per the course submission structure
(code / data / docs / presentation / results), but rather than duplicate content,
everything is consolidated in one place to avoid the report and the code
documentation drifting out of sync with each other.
